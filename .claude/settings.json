{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE 'assembleDebug|assembleRelease|assembleXCFramework'; then bash .claude/hooks/pre-build-check.sh; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'twine.txt'; then echo '[hook] twine.txt edited — run: make strings'; fi"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'twine.txt'; then echo '[hook] twine.txt written — run: make strings'; fi"
          }
        ]
      }
    ]
  }
}
